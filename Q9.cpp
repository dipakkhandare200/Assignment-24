//Write functions using function overloading to find a maximum of two numbers and both the numbers can be integer or real
#include<iostream>
int maxmnumber(int t1,int t2);
using namespace std;
int main()
{
    float n1,n2;
    cout<<"Enter the Rows you want"<<endl;
    cin>>n1>>n2;
    maxmnumber(n1,n2);
}
int maxmnumber(int t1,int t2)
{
    if((t1<0&&t1<0))||((t2<0&&t2>0))
    {
        cout<<"its real
    }
        cout<<endl;
    }
}
